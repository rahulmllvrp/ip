plugins {
    id 'java'
    id 'application'
}

repositories {
    mavenCentral()
}

dependencies {
    // No test dependencies for now
}

application {
    mainClass.set("sage.main.Sage")
}

run{
    standardInput = System.in
}
